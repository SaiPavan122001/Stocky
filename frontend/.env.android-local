# Used only for `npm run build:android-local` (see package.json), which
# Capacitor packages into the Android app. 10.0.2.2 is the Android
# emulator's alias for your machine's localhost, where backend-go runs.
# For a physical device on the same Wi-Fi, override with your machine's
# LAN IP instead (e.g. VITE_API_BASE=http://192.168.1.23:8080/api/v1).
VITE_API_BASE=http://10.0.2.2:8080/api/v1
